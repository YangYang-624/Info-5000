# Setup · Introduction

## Recorded Setup Notes

- Implemented and ran a bounded adaptive trust-region BO controller in the existing lumo environment. The controller preserved the active comparison contract: baseline ax-pybamm-fastcharge with headline comparator BO_3step_aggressive, required paper-facing metrics Q30/plating_loss/sei_growth/total_lli, and the same five-scenario followup_v1 robustness bundle used by the incumbent line.
- Implemented a feasibility-aware upgrade of the previous trust-region BO controller in the existing lumo environment. The new controller preserved the active comparison contract: baseline ax-pybamm-fastcharge with headline comparator BO_3step_aggressive, required metrics Q30/plating_loss/sei_growth/total_lli, and the same five-scenario followup_v1 robustness bundle. The mechanism change was localized to the proposal rule: a dual-model controller learned both search_objective and guard_pass_rate, then used guard-feasibility-weighted expected improvement to pick new candidates on the same three-parameter protocol surface.
- Used the existing lumo CUDA environment. Reused the ua_trbo_v1 package but changed the active anchor to UA3_TRBO_3.00C_rest10m_3.95C, narrowed the first-rate/rest-time neighborhood, extended third-stage current search up to 4.15C, and biased candidate sampling toward the local upper frontier while keeping the same four-metric comparison contract and robust guard-first ranking rule.
- Used the existing lumo CUDA environment and the new ua_vtbo_v1 package on the dedicated run branch. The search family exposed four parameters: first_rate, rest_minutes, third_rate, and tail_rate, while keeping a fixed 3-minute tail window as the first voltage-aware proxy. The comparison contract, guard-first ranking rule, and baseline references were unchanged.
- Used the accepted ax-pybamm-fastcharge baseline contract in the `lumo` environment, extended with backward-compatible native PyBaMM charge-step support. The main run evaluated the ua_evtbo_v1 guard-then-trigger protocol family against the current incumbent UA4_VTBO_3.00C_rest10m_4.25C_tail2.40C under the followup_v1 five-scenario robust bundle.
- Use the accepted PyBaMM fast-charge baseline contract on the existing lumo environment. Search on the followup_v1 five-scenario bundle with the EVTBO incumbent UA4_EVTBO_3.00C_rest10m_4.30C_v4p16 as the anchor. PSMS-v2 adds a separate entry-rate dimension and a sequence-feasibility prior inside the trust-region acquisition.
- Used the same UA4 anchor family and followup_v1 scenario set as the PSMS-v2 larger-budget search, but enabled the lightweight learned feasibility gate discovered in smoke tests. The formal search used search_budget=12 and preserved the confirmed comparison metrics Q30, plating_loss, sei_growth, and total_lli.
- Used the accepted baseline contract `ax-pybamm-fastcharge` / `bo_3step_aggressive` and rebuilt the two warm-start candidates from stored search points in `ua_psms_v5_warmstart_smoke12_summary.json`. Repaired the focused validation runner so it reuses the shared parameter-update path and only passes `charge_steps` when the simulator signature supports it.
